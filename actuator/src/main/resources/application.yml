management:
  server:
    port: 8080
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
      show-components: always
#      show-details: always

info:
  app:
    name: hello-actuator
    companay: daeun

logging:
  level:
    hello.controller: debug
